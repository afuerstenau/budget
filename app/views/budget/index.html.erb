<h1>Budget für <%= l(@monthname, format: "%B") %></h1>
<p>
	<table>
	  <tr>
	    <th>Kategorie</th>
	    <th>Betrag</th>
	  </tr>

	<% @sums.each do |category_id, sum| %>
	  <tr>
	    <td><%= Category.find(category_id).name %></td>
	    <td style="text-align: right;"><%= number_to_currency(sum) %></td>
	  </tr>
	<% end %>
</p>
